// Ambient declaration for global CSS side-effect imports (e.g. app/globals.css).
// Previously provided transitively by vite/client types, which were removed
// with the legacy Vite app.
declare module "*.css";
